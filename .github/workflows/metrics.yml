# GitHub Metrics — Auto-generate SVG infographics for your profile
# Based on https://github.com/lowlighter/metrics

name: Metrics
on:
  schedule: [{cron: "0 0 * * *"}]
  workflow_dispatch:
  push:
    branches: [main]

jobs:

  # ═══════════════════════════════════════════════════════════
  # 📊 Left Column — Topics + Languages + WakaTime + Habits + Calendar
  # ═══════════════════════════════════════════════════════════
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 📊 General Metrics
        uses: lowlighter/metrics@latest
        with:
          filename: github-metrics.svg
          token: ${{ secrets.METRICS_TOKEN }}

          # ═══ User info ═══
          user: Sowndhar-2005
          template: classic
          config_timezone: Asia/Kolkata

          # Exact section order: Starred Topics (Icons) → Languages → WakaTime → Coding Habits → Calendar
          config_order: topics, languages, wakatime, habits, isocalendar

          # ═══ Base content completely removed (No Activity, Repositories, Community stats) ═══
          base: ""

          # ═══ Starred Topics (with Icons) ═══
          plugin_topics: yes
          plugin_topics_mode: icons
          plugin_topics_limit: 0
          plugin_topics_sort: stars

          # ═══ Most Used Languages ═══
          plugin_languages: yes
          plugin_languages_analysis_timeout: 15
          plugin_languages_categories: markup, programming
          plugin_languages_colors: github
          plugin_languages_limit: 8
          plugin_languages_sections: most-used
          plugin_languages_threshold: 0%

          # ═══ WakaTime ═══
          plugin_wakatime: yes
          plugin_wakatime_token: ${{ secrets.WAKATIME_TOKEN }}
          plugin_wakatime_days: 7
          plugin_wakatime_sections: time, projects, projects-graphs, languages, languages-graphs, editors, os
          plugin_wakatime_limit: 5

          # ═══ Coding Habits and Activity ═══
          plugin_habits: yes
          plugin_habits_from: 200
          plugin_habits_days: 14
          plugin_habits_facts: yes
          plugin_habits_charts: yes

          # ═══ Contribution Calendar (isometric 3D) ═══
          plugin_isocalendar: yes
          plugin_isocalendar_duration: half-year

  # ═══════════════════════════════════════════════════════════
  # 🦑 Right Column — Repos + Anilist + Music
  # ═══════════════════════════════════════════════════════════
  medias:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 🦑 Medias & Projects
        uses: lowlighter/metrics@latest
        with:
          filename: medias.svg
          token: ${{ secrets.METRICS_TOKEN }}

          # ═══ User info ═══
          user: Sowndhar-2005
          template: classic
          config_timezone: Asia/Kolkata
          config_order: repositories, anilist, music

          # ═══ No base content ═══
          base: ""

          # ═══ Featured Repositories (your 3 projects) ═══
          plugin_repositories: yes
          plugin_repositories_featured: Sowndhar-2005/the-Honored-One, Sowndhar-2005/Student-Performance-Analysis-Dashboard, Sowndhar-2005/DrugGuard-Detection-System

          # ═══ Anilist (anime & manga) ═══
          plugin_anilist: yes
          plugin_anilist_user: Master466
          plugin_anilist_medias: anime
          plugin_anilist_sections: favorites, characters
          plugin_anilist_limit: 2
          plugin_anilist_limit_characters: 22

          # ═══ Music (YouTube Music playlist) ═══
          plugin_music: yes
          plugin_music_provider: youtube
          plugin_music_mode: playlist
          plugin_music_playlist: https://music.youtube.com/playlist?list=PLGxDBKgU66aU&si=i3QZ7PCSdba_-XwJ
          plugin_music_limit: 4
